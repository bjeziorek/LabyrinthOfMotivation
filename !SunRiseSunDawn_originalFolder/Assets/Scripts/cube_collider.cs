using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class cube_collider : MonoBehaviour {

	public GameObject gwiazdeczki_mgr_handler;

	//void OnTriggerEnter(Collider collision){
	/*	print ("cube collider trigger");
		transform.parent.parent.GetComponent<SphereCollider> ().enabled = false;
		transform.parent.parent.GetComponent<gwiazdeczka_obj> ().Kolizja ();
		print ("cube collider trigger "+transform.parent.parent.GetComponent<literka> ().GetABC ());
		//gwiazdeczki_mgr_handler.GetComponent<gwiazdeczki_mgr> ().ColInfo (gameObject);
		Destroy (gameObject);
		*/
	//}

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
